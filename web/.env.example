DATABASE_URL="file:./dev.db"
# openssl rand -base64 32  (iron-session needs 32+ chars)
SESSION_SECRET="change-me-to-a-long-random-string-at-least-32-chars"

# Feedback email (optional — without RESEND_API_KEY the API returns a clear error)
RESEND_API_KEY=
FEEDBACK_TO_EMAIL=you@example.com
FEEDBACK_FROM_EMAIL=onboarding@resend.dev
