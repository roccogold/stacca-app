# Stacca implementation status

Implemented:

1. **`web/`** — Next.js 16, Prisma 5 + SQLite, `iron-session`, Resend feedback API, Italian UI + **calendar grid** on `/mese`, **Stacca** brand + logo.
2. **Routes** — `/login`, `/`, `/aggiungi`, `/mese`, `/profilo`, `/profilo/feedback`; APIs for auth, entries CRUD, feedback.
3. **`mockups/`** — Static HTML/CSS reference with **Stacca** branding, **Invia feedback** row + `feedback.html` preview.

Still planned: Google Sheets export/sync, real passwords, month lock / “invia mese”.
